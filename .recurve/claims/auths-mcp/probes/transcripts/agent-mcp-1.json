{
  "//": "AGENT-MCP-1 accept transcript: an in-scope, in-budget fs.read round-trips through the gateway — real downstream result + a receipt auths verify accepts. Frozen replay (PRD §7). PLACEHOLDER shape; the recorded transcript is pinned in the burndown.",
  "scenario": "broker",
  "grant": { "scope": ["fs.read"], "budget": "$5.00", "ttl": "30m" },
  "calls": [
    { "tool": "read_file", "args": { "path": "README.md" }, "cost_cents": 0, "expect": "allowed", "expect_receipt": true }
  ]
}
