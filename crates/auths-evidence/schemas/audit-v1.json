{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://auths.dev/schemas/audit-v1.json",
  "title": "AuditV1 (audit/v1)",
  "description": "The versioned, typed output of one spend re-derivation — shared by the gateway CLI, the tool servers, and every language binding.",
  "type": "object",
  "required": ["version", "verdict", "code", "consistent", "records", "settled_cents"],
  "properties": {
    "version": { "const": "audit/v1" },
    "verdict": {
      "type": "object",
      "required": ["verdict"],
      "properties": {
        "verdict": {
          "type": "string",
          "enum": ["consistent", "tampered-proof", "cost-mismatch", "counterparty-mismatch", "budget-mismatch", "chain-break", "revoked"]
        }
      },
      "description": "The typed audit verdict (internally tagged, kebab-case), carrying the failing check's own fields. Note: the Consistent variant's internal tag is 'consistent' while its stable machine code (below) is 'self-consistent' — an offline audit proves self-consistency, not completeness."
    },
    "code": {
      "type": "string",
      "enum": ["self-consistent", "tampered-proof", "cost-mismatch", "counterparty-mismatch", "budget-mismatch", "chain-break", "revoked"]
    },
    "consistent": { "type": "boolean" },
    "records": { "type": "integer", "minimum": 0 },
    "settled_cents": { "type": "integer", "minimum": 0 },
    "counterparty": {
      "type": "string",
      "description": "The verified counterparty the last metered call settled with (Auths-Settle-Ref, cross-checked against the rail response) — a proven field consumers act on, never receipt.charge_ref. Absent when nothing metered settled."
    },
    "completeness": {
      "type": "string",
      "description": "Whether completeness was proven. An offline audit re-derives self-consistency but cannot prove the log is complete (a $0/refused tail truncation is invisible) — 'unproven-offline' for a consistent offline audit; absent otherwise."
    },
    "checkpoint": {
      "type": "object",
      "required": ["records", "settled_cents", "binding"],
      "additionalProperties": false,
      "properties": {
        "records": { "type": "integer", "minimum": 0 },
        "settled_cents": { "type": "integer", "minimum": 0 },
        "binding": { "type": "string" }
      },
      "description": "The resumable end state a checkpointing caller stores for its next run."
    },
    "treasury": {
      "type": "object",
      "required": ["fleet", "count", "cumulative_cents", "at"],
      "additionalProperties": false,
      "properties": {
        "fleet": { "type": "string" },
        "count": { "type": "integer", "minimum": 0 },
        "cumulative_cents": { "type": "integer", "minimum": 0 },
        "at": { "type": "string", "format": "date-time" }
      },
      "description": "The verified treasury checkpoint trail's final state, when a trail was supplied."
    },
    "freshness": {
      "type": "object",
      "required": ["status"],
      "additionalProperties": false,
      "description": "AWN witness-tier freshness, when a quorum anchor was resolved (a separate labeled result, never folded into the verdict).",
      "properties": {
        "status": { "type": "string", "enum": ["fresh", "stale", "unanchored"] },
        "anchorIndex": { "type": "integer", "minimum": 0 },
        "bundleIndex": { "type": "integer", "minimum": 0 }
      }
    }
  },
  "additionalProperties": false
}
