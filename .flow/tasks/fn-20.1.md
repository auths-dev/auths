# fn-20.1 Create auths-pairing-protocol crate skeleton with protocol error types

## Description
## Create auths-pairing-protocol crate skeleton with protocol error types

### Crate location

`crates/auths-pairing-protocol/`

### Cargo.toml

```toml
[package]
name = "auths-pairing-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Transport-agnostic pairing protocol for auths identity system"

[dependencies]
auths-crypto = { workspace = true }
x25519-dalek = { version = "2", features = ["static_secrets"] }
rand = "0.8"
base64.workspace = true
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
zeroize = { version = "1.8.1", features = ["serde", "derive"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror.workspace = true
```

**NO:** `qrcode`, `axum`, `tower-http`, `mdns-sd`, `reqwest`, `schemars`.

### Error type — protocol errors only

Create `src/error.rs` with protocol-only errors (10 of the 16 current variants):

```rust
#[derive(Debug, thiserror::Error)]
pub enum ProtocolError {
    #[error("pairing token expired")]
    Expired,
    #[error("invalid signature")]
    InvalidSignature,
    #[error("session already consumed")]
    SessionConsumed,
    #[error("key exchange failed: {0}")]
    KeyExchangeFailed(String),
    #[error("key generation failed: {0}")]
    KeyGenFailed(String),
    #[error("RNG failure: {0}")]
    RngFailed(String),
    #[error("invalid pairing URI: {0}")]
    InvalidUri(String),
    #[error("invalid short code: {0}")]
    InvalidShortCode(String),
    #[error("short code not found: {0}")]
    ShortCodeNotFound(String),
    #[error("serialization error: {0}")]
    Serialization(String),
}

impl From<serde_json::Error> for ProtocolError {
    fn from(e: serde_json::Error) -> Self {
        ProtocolError::Serialization(e.to_string())
    }
}
```

Transport-specific errors (`RelayError`, `LocalServerError`, `MdnsError`, `NoPeerFound`, `LanTimeout`, `QrCodeFailed`) are NOT included — they stay in the CLI transport layer.

### Module structure

```
crates/auths-pairing-protocol/
  Cargo.toml
  src/
    lib.rs
    error.rs
```

### Workspace registration

- Add to workspace `members` list in root `Cargo.toml`
- Add `auths-pairing-protocol = { path = "crates/auths-pairing-protocol" }` to `[workspace.dependencies]`

### Files
- Create: `crates/auths-pairing-protocol/Cargo.toml`
- Create: `crates/auths-pairing-protocol/src/lib.rs`
- Create: `crates/auths-pairing-protocol/src/error.rs`
- Modify: `Cargo.toml` (workspace members + deps)

### Verification
```bash
cargo check -p auths-pairing-protocol
```
## Create auths-pairing-protocol crate skeleton with protocol error types

Create the new `auths-pairing-protocol` crate with minimal structure and the protocol-only error type.

### Crate location

`crates/auths-pairing-protocol/`

### Cargo.toml

```toml
[package]
name = "auths-pairing-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Transport-agnostic pairing protocol for auths identity system"

[dependencies]
auths-crypto = { workspace = true }
x25519-dalek = { workspace = true }
rand = { workspace = true }
base64 = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
zeroize = { workspace = true, features = ["derive"] }
chrono = { workspace = true, features = ["serde"] }
thiserror = { workspace = true }
```

NO: axum, tower-http, mdns-sd, qrcode, reqwest.

### Error type split

Create `src/error.rs` with protocol-only errors (extracted from `auths-core/src/pairing/error.rs`):

```rust
#[derive(Debug, thiserror::Error)]
pub enum ProtocolError {
    #[error("pairing token expired")]
    Expired,
    #[error("invalid signature")]
    InvalidSignature,
    #[error("session already consumed")]
    SessionConsumed,
    #[error("key exchange failed: {0}")]
    KeyExchangeFailed(String),
    #[error("key generation failed")]
    KeyGenFailed,
    #[error("RNG failure")]
    RngFailed,
    #[error("invalid pairing URI: {0}")]
    InvalidUri(String),
    #[error("invalid short code")]
    InvalidShortCode,
    #[error("serialization error: {0}")]
    Serialization(String),
}
```

Transport-specific variants (`RelayError`, `LocalServerError`, `MdnsError`, `NoPeerFound`, `LanTimeout`, `QrCodeFailed`) stay in auths-core.

### Module structure

```
crates/auths-pairing-protocol/
  Cargo.toml
  src/
    lib.rs
    error.rs
```

### Workspace registration

Add to workspace `Cargo.toml`:
- `members` list
- `[workspace.dependencies]` entry for `auths-pairing-protocol`

### Files
- Create: `crates/auths-pairing-protocol/Cargo.toml`
- Create: `crates/auths-pairing-protocol/src/lib.rs`
- Create: `crates/auths-pairing-protocol/src/error.rs`
- Modify: `Cargo.toml` (workspace members + deps)

### Verification
```bash
cargo check -p auths-pairing-protocol
```
## Acceptance
- [ ] `crates/auths-pairing-protocol/` crate exists and compiles
- [ ] `ProtocolError` contains 10 protocol-specific variants (no transport errors)
- [ ] `From<serde_json::Error>` impl for ProtocolError
- [ ] Dependencies minimal: auths-crypto, x25519-dalek, rand, base64, serde, zeroize, chrono, thiserror
- [ ] No qrcode, axum, tower-http, mdns-sd dependencies
- [ ] Registered as workspace member with workspace dependency entry
- [ ] `cargo check -p auths-pairing-protocol` passes
## Done summary
Created auths-pairing-protocol crate with error types, registered in workspace
## Evidence
- Commits:
- Tests:
- PRs: