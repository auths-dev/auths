# fn-10.4 Refactor CLI claim.rs to use SDK submit_claim workflow

## Description
## Refactor CLI claim.rs to use SDK submit_claim workflow

### Context

After fn-10.3, the SDK has `submit_platform_claim()`. Now thin out the CLI handler.

### What to Do

**1. Update `crates/auths-cli/src/commands/id/claim.rs`:**

- Remove the inline `submit_claim()` function that does HTTP directly
- Replace with call to `auths_sdk::workflows::claim::submit_platform_claim()`
- Wire up `HttpRegistryClient` from the CLI composition root
- Keep all CLI output formatting

**2. Handle error mapping at CLI boundary:**

- `ClaimError::IdentityNotRegistered` -> user-friendly message suggesting `auths register`
- `ClaimError::NetworkError` -> anyhow context with endpoint info

### Key Files

- `crates/auths-cli/src/commands/id/claim.rs`
- `crates/auths-cli/src/factories/mod.rs`

### Constraints

- CLI output must be identical
- `cargo nextest run --workspace` passes
## Acceptance
- [ ] `claim.rs` has zero `reqwest` imports
- [ ] `claim.rs` has zero HTTP status code handling
- [ ] `claim.rs` calls SDK `submit_platform_claim()` function
- [ ] CLI output for success, not-registered, and error cases is unchanged
- [ ] `cargo nextest run --workspace` passes
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` passes
## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
