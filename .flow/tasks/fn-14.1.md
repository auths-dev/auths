# fn-14.1 Crate skeleton and trait boundary

## Description
# Task: Crate Skeleton and Trait Boundary

Create the `auths-keri` workspace crate with the `CesrCodec` trait, error types, and module stubs.

## What to do

1. Create `crates/auths-keri/Cargo.toml` following the workspace pattern (see `auths-crypto/Cargo.toml` for reference):
   - `edition = "2024"`, `version.workspace = true`, `license.workspace = true`
   - `[lints.clippy] print_stdout = "deny"`, `print_stderr = "deny"`
   - Dependencies: `cesride = "0.6"`, `auths-verifier = { workspace = true, features = ["native"] }`, `auths-crypto.workspace = true`, `base64.workspace = true`, `serde = { version = "1", features = ["derive"] }`, `serde_json = "1"`, `thiserror.workspace = true`, `blake3 = "1.5"`, `hex = "0.4.3"`
   - Dev-deps: `proptest = "1.4"`
   - NOTE: `json-canon` is NOT needed (SAID uses `serde_json`, not JCS)

2. Add `"crates/auths-keri"` to `[workspace.members]` in root `Cargo.toml`

3. Create `src/lib.rs` with module declarations and re-exports (modules can be stubs with `// TODO` for now)

4. Create `src/error.rs` with `KeriTranslationError` thiserror enum following the pattern at `auths-id/src/keri/validate.rs:40-87`

5. Create `src/codec.rs` with the `CesrCodec` trait definition and `CesrV1Codec` zero-sized struct (implementation deferred to fn-14.2)

6. Create stub files: `src/said.rs`, `src/version.rs`, `src/event.rs`, `src/stream.rs`, `src/roundtrip.rs`

7. Create test scaffold: `tests/integration.rs` (`mod cases;`) and `tests/cases/mod.rs`

## Key files to reference

- `crates/auths-crypto/Cargo.toml` -- Cargo.toml template
- `crates/auths-verifier/src/lib.rs:1-10` -- lint configuration pattern
- `crates/auths-id/src/keri/validate.rs:40-87` -- thiserror enum pattern
- `Cargo.toml:3-18` -- workspace members list

## Design decisions

- `CesrCodec` is a trait (not free functions) to enable mock testing and future code-table versions
- `CesrV1Codec` is zero-sized -- no runtime cost
- Error type uses `thiserror` per CLAUDE.md (no `anyhow` in domain crates)
## Acceptance
- `cargo build -p auths-keri` compiles with zero warnings
- `cargo test -p auths-keri` runs (empty test suite is fine)
- `cargo clippy -p auths-keri --all-targets -- -D warnings` passes
- Crate is listed in workspace `members` in root `Cargo.toml`
- `CesrCodec` trait is defined with `encode_pubkey`, `encode_indexed_signature`, `encode_digest`, `decode_qualified` methods
- `KeriTranslationError` has variants: `EncodingFailed`, `DecodingFailed`, `SerializationFailed`, `SaidMismatch`, `VersionStringError`, `MissingField`, `SignatureParseError`, `RoundTripFailed`
## Done summary
- Created `auths-keri` crate with full module structure (codec, error, event, said, stream, version, roundtrip)
- Defined `CesrCodec` trait with `encode_pubkey`, `encode_indexed_signature`, `encode_digest`, `decode_qualified`
- Defined `KeriTranslationError` with all 8 required variants
- Added test scaffold (`tests/integration.rs` + `tests/cases/mod.rs`)
- Why: establishes the crate boundary and public API surface for the translation layer
- Verification: `cargo build -p auths-keri` (0 warnings), `cargo test -p auths-keri`, `cargo clippy -p auths-keri --all-targets -- -D warnings` all pass
## Evidence
- Commits: 59e6349674aae41207f8e3f91229ed171b68fa91
- Tests: cargo test -p auths-keri, cargo clippy -p auths-keri --all-targets -- -D warnings
- PRs:
