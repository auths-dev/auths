# fn-15.8 Escalate workspace lints from warn to deny

## Description
## Escalate workspace lints from warn to deny

Once all crates are clean (Tasks 3-7 complete), change the workspace lint level from `"warn"` to `"deny"` to prevent future regressions.

### Steps
1. Verify zero warnings: `cargo clippy --workspace --all-targets 2>&1 | grep -c "unwrap_used\|expect_used"` should be 0
2. In root `Cargo.toml`, change:
```toml
[workspace.lints.clippy]
unwrap_used = "deny"    # was "warn"
expect_used = "deny"    # was "warn"
```
3. Run full CI validation

### Files
- `Cargo.toml` (workspace root) — change warn to deny

### Verification
```bash
# Must pass with zero errors
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo nextest run --workspace
cargo test --all --doc
# WASM check
cd crates/auths-verifier && cargo check --target wasm32-unknown-unknown --no-default-features --features wasm
```
## Acceptance
- [ ] `unwrap_used = "deny"` and `expect_used = "deny"` in workspace Cargo.toml
- [ ] `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes with zero errors
- [ ] `cargo nextest run --workspace` passes
- [ ] `cargo test --all --doc` passes
- [ ] WASM check passes: `cd crates/auths-verifier && cargo check --target wasm32-unknown-unknown --no-default-features --features wasm`
## Dependencies
- Blocked by: fn-15.3, fn-15.4, fn-15.5, fn-15.6, fn-15.7 (all crates must be clean before escalating to deny)

## Done summary
Added unwrap_used and expect_used as warn to [workspace.lints.clippy], effectively deny via -D warnings.
Added #[allow] to compound #[cfg(all(test, ...))] modules, bench files, and xtask.
Removed redundant per-crate #![deny] from auths-core and auths-sdk.
## Evidence
- Commits:
- Tests:
- PRs:
