# fn-11.3 Eliminate crypto duplication in auths-mobile-ffi

## Description
## Eliminate crypto duplication in auths-mobile-ffi

### Context

`auths-mobile-ffi` at `crates/auths-mobile-ffi/src/lib.rs` (148 lines) reimplements KERI inception, Ed25519 signing, SAID computation, and DID generation directly using `ring`, instead of depending on `auths-crypto` or `auths-core`. It has its own `[workspace]` declaration (excluded from main workspace) and independently pins `ring = "0.17.14"`, `blake3`, `serde_json`, etc. This creates drift risk — if crypto logic changes in the core, the mobile FFI silently diverges.

### What to Do

1. Add `auths-crypto` as a dependency of `auths-mobile-ffi` (with `default-features = false`, only the `native` feature for ring).

2. Replace duplicated crypto operations:
   - Key generation → use `RingCryptoProvider` / `SyncCryptoProvider`
   - Ed25519 signing → use `SyncCryptoProvider::sign()`
   - Ed25519 verification → use `SyncCryptoProvider::verify()`
   - SAID computation → reuse from auths-core/auths-id if available, or extract to auths-crypto

3. Keep `IcpEvent` struct and KERI serialization logic — check if `auths-id` exports these types. If so, depend on the relevant crate. If not, keep the local version but add a TODO to extract it.

4. Remove direct `ring` dependency from auths-mobile-ffi (it comes transitively through auths-crypto).

5. Verify cross-compilation still works: `cargo check --target aarch64-apple-ios` from within the auths-mobile-ffi crate directory.

### Key Files

- `crates/auths-mobile-ffi/Cargo.toml` — independent workspace, pins ring directly
- `crates/auths-mobile-ffi/src/lib.rs` — duplicated crypto logic
- `crates/auths-crypto/src/provider.rs` — SyncCryptoProvider (from fn-11.1)
- `crates/auths-crypto/Cargo.toml` — to verify feature flags

### Constraints

- auths-mobile-ffi remains excluded from the main workspace (its own `[workspace]`)
- Must not pull in tokio (use SyncCryptoProvider, not async CryptoProvider)
- Must not pull in git2
- ring version must match workspace pin (0.17.14)
- All existing inline tests in auths-mobile-ffi must pass
## Acceptance
- [ ] `auths-mobile-ffi` depends on `auths-crypto` (not raw `ring`)
- [ ] No duplicated Ed25519 key generation, signing, or verification code
- [ ] No direct `ring` dependency in auths-mobile-ffi Cargo.toml
- [ ] `cargo check` in `crates/auths-mobile-ffi/` passes
- [ ] `cargo check --target aarch64-apple-ios` passes from the crate directory
- [ ] No tokio or git2 in the dependency tree (`cargo tree` check)
- [ ] Existing inline tests pass
## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
