# fn-12.2 auths-core: expand test-utils feature

## Description

Expand the existing `test-utils` feature in `auths-core` to include test helpers that implement traits defined in `auths-core`.

### What to move

From `crates/auths-test-utils/src/fakes/id.rs`:
- `DeterministicUuidProvider` (implements `UuidProvider` from `auths-core/src/ports/id.rs:7`)

From `crates/auths-test-utils/src/storage_fakes.rs`:
- `InMemoryStorage` (implements `BlobReader`, `BlobWriter`, `RefReader`, `RefWriter`, `EventLogReader`, `EventLogWriter` from `auths-core/src/ports/storage/`)

Note: `InMemoryStorage` also uses `auths_verifier::keri::Prefix`. Since `auths-core` already depends on `auths-verifier`, this import works.

### Where to put it

Add to existing `crates/auths-core/src/testing/mod.rs` which already has `MemoryStorage`, `MemoryKeychainHandle`, `TestIdentityBuilder`, `TestPassphraseProvider`.

### Cargo.toml changes

```toml
[features]
test-utils = ["auths-crypto/test-utils"]  # chain the feature
```

### Files to modify

- `crates/auths-core/Cargo.toml` — update `test-utils` feature to chain `auths-crypto/test-utils`
- `crates/auths-core/src/testing/mod.rs` — add moved helpers

### Verification

```bash
cargo build -p auths-core
cargo build -p auths-core --features test-utils
cargo nextest run -p auths-core
```
## Acceptance
- [ ] `test-utils` feature chains to `auths-crypto/test-utils`
- [ ] `DeterministicUuidProvider` available via `auths_core::testing::DeterministicUuidProvider`
- [ ] `InMemoryStorage` available via `auths_core::testing::InMemoryStorage`
- [ ] Existing `MemoryStorage`, `TestIdentityBuilder` etc. still work
- [ ] `cargo build -p auths-core` passes (without test-utils)
- [ ] `cargo build -p auths-core --features test-utils` passes
- [ ] `cargo nextest run -p auths-core` passes
## Done summary
- Added `DeterministicUuidProvider` to `auths-core/src/testing/deterministic_uuid.rs`
- Added `InMemoryStorage` (6 storage port traits) to `auths-core/src/testing/in_memory_storage.rs`
- Chained `test-utils` feature to `auths-crypto/test-utils`
- All pre-commit hooks pass (fmt, clippy, deny)
## Evidence
- Commits: c4d4c20d6f8193c92827b0a2ad5098bf34031cb5
- Tests: cargo build -p auths-core --all-features
- PRs:
